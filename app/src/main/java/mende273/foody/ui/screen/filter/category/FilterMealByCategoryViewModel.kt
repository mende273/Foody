package mende273.foody.ui.screen.filter.category

import androidx.lifecycle.ViewModel

class FilterMealByCategoryViewModel : ViewModel() {

    fun requestData(categoryName: String) {
        if (categoryName.isNotEmpty()) {
            // TODO
        } else {
            // TODO
        }
    }
}
